package com.app.notesculture.view.ui.infra;

import com.app.notesculture.model.SubjectDetailResponseData;

public interface CustomClickListener {
    void subCardClicked(SubjectDetailResponseData subjectDetailResponseData);
}
